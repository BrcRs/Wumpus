package eu.su.mas.dedaleEtu.mas.behaviours.fsm;

//public enum FSMReturnValues { // Can't use enum: the return values must be integers
public class FSMReturnValues {
	static final public int TRUE = 2;
	static final public int FALSE = 1;
}
